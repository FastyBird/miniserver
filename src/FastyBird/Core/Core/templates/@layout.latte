<!DOCTYPE html>
<html lang="en" class="h-full min-h-full max-h-full w-full min-w-full max-w-full m-0 p-0">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<link rel="icon" href="{='favicon.ico'|vite}/" />

	<title>{ifset title}{include title|stripHtml} | {/ifset}FastyBird IoT</title>

	{if $vite->isEnabled()}
		<script type="module" src="{='@vite/client'|vite}"></script>
	{/if}
</head>

<body class="h-full min-h-full max-h-full w-full min-w-full max-w-full m-0 p-0">
	<div n:foreach="$flashes as $flash" n:class="flash, $flash->type">{$flash->message}</div>

	<div id="app" class="h-full min-h-full max-h-full w-full min-w-full max-w-full m-0 p-0">
		{include content}
	</div>

	<script src="{='index.html'|vite}" type="module"></script>
	{block scripts}{/block}
</body>
</html>
